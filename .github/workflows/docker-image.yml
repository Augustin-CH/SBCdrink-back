name: Docker Image CI
      
on:
  push:
    branches:
    - cicd-1

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      REGISTRY_URL: ${{ secrets.CI_REGISTRY | awk '{print tolower($0)}' }}${{ github.repository | awk '{print tolower($0)}' }}/
      IMAGE_NAME: ${{ github.ref_name | awk '{print tolower($0)}' }}:${{ github.sha | awk '{print tolower($0)}' }}
    steps:
    - uses: actions/checkout@v3
    - run: echo '${{ toJSON(github) }}'
    - run: echo "image name $IMAGE_NAME"
    - run: echo "image name $REGISTRY_URL"
    - name: Build and push the Docker image
      run: |
        docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
        DOCKER_BUILDKIT=1 docker build -t $IMAGE_NAME .
        docker push $REGISTRY_URL$IMAGE_NAME
